openapi: 3.1.0
info:
  title: Minimal 3.1
  version: 0.1.0
paths:
  /ping:
    get:
      operationId: ping
      tags: [system]
      responses:
        "200":
          description: pong
webhooks:
  unused:
    post:
      responses:
        "200": { description: ok }
